# rpi-ask-user

Structured questions for workflows that need an explicit user decision.
The `ask_user` tool returns validated questions and a selector-shaped `ui`
payload so hosts can render native single/multi-select controls.
