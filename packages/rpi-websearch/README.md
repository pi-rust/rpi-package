# rpi-websearch

Keyless web reference search with bounded result count and output. DuckDuckGo
Instant Answer results are supplemented by Wikipedia when available. The tool
returns model-readable titles, URLs, snippets, source labels, and a structured
`details` object containing the query and any failed fallback engine.
